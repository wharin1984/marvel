import { Alert } from "react-bootstrap";

const ErrorMessage = () => {
    return (
        <Alert variant="danger">
            <Alert.Heading>Error component</Alert.Heading>
        </Alert>
    );
}

export default ErrorMessage;